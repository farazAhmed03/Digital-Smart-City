name: CI-CD Production Pipeline

on:
  push:
    branches: [ "main" ] 

jobs:
  production-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    # 3. AWS Credentials
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ vars.AWS_REGION }}

    # 4. AWS ECR  login
    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    # 5. Docker Build aur ECR Registry Image Push
    - name: Build, tag, and push image to Amazon ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: my-web-app 
        IMAGE_TAG: ${{ github.sha }} 
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV

    # # 6. AWS EKS (Kubernetes Cluster)  connect
    # - name: Update Kubeconfig for EKS
    #   run: |
    #     aws eks update-kubeconfig --region us-east-1 --name my-eks-cluster

    # # 7. K8s Deploy: Kubernetes par naya image apply
    # - name: Deploy to Kubernetes
    #   run: |
    #     # K8s ki deployment file mein image tag ko automatic update
    #     sed -i "s|image:.*|image: ${{ env.image }}|g" k8s/deployment.yaml
        
    #     # Kubernetes files ko apply
    #     kubectl apply -f k8s/deployment.yaml
    #     kubectl apply -f k8s/hpa.yaml # HPA file ko apply
